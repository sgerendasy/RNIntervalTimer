import React from 'react';
import {Container, RowContainer, Text} from '../components/CustomComponents';

const IntervalSetScreen = ({route, navigation}) => {

    return (
        <Container>
            <Text>Interval Set Screen</Text>
        </Container>
    );
};

export default IntervalSetScreen;
